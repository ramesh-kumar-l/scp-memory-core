# 08 — Active Phase

**Last updated:** 2026-06-20 · **Read this first, every session.**

> Doubles as the project's **active-context** save state (05 working agreement).

## Active Phase: 8 — Android Reference App 🚧 in progress (user-approved 2026-06-20)

> Phase 7 (Admin Console) is complete; see the archived section below. The user
> explicitly approved Phase 8 on 2026-06-20 and it is now the active phase.

### Phase 8 location & scope
- **Separate repo/folder:** `E:\ClaudeProjects\SCPMemoryEngine—AndroidReferenceApp`
  (NOT inside `scp-memory-core`). Built to `android-app-system-prompt.md` (§1–§20).
- **Stack:** Kotlin, Jetpack Compose + Material 3, MVVM + Repository, Hilt,
  Navigation Compose, Coroutines/Flow, Retrofit + kotlinx-serialization, Room,
  DataStore, Coil. 74 Kotlin files, **all < 300 lines** (largest 151); strict
  modularity held.
- **Architecture decision (user choice): Hybrid.** Remote-first against the engine
  HTTP API (§9) when reachable; transparent fallback to a local **Room** store when
  not — so the app demos standalone. A clearly-isolated offline stand-in
  (`LocalRetrievalEngine`/`LocalTrustEngine`) reproduces the engine's signal/trust
  *contract* only for the no-backend path (engine stays authoritative when present).
- **Seeding decision (user choice): write the device Room DB directly** via
  `adb run-as sqlite3` (debug build). PC tooling in `seed/`:
  `seed_device.py` (+ `adb.py`, `build_sql.py`, `seed_data.py`, `make_images.py`)
  and `reset_device.py`. Seeds 14 memories + audit + gallery images into namespace
  `user:demo`, visible on first launch.
- **3 Golden Use Cases:** Semantic Gallery (photos/receipts/screenshots, querying by
  event), Personal Finance & Receipts, AI Assistant Memory (contradiction +
  consolidation/summary trust story). Mapped in `docs/golden-use-cases.md`.
- **Screens (6 + detail/audit/store):** Dashboard, Search (signal Explain panel),
  Explore, Memory Detail, Trust Explorer (confidence gauge + breakdown), Settings;
  Score Bar primitive reused everywhere; Loading/Empty/Error/No-results states;
  WCAG-AA intent (color+text, TalkBack descriptions, 48dp, font scale).
- **Docs:** `README.md` (production), `QuickStarterGuide.md` (newbie zero-to-run),
  `docs/golden-use-cases.md`. JVM unit tests: `/metrics` parser + trust engine.

### Phase 8 remaining / not yet done
- Build/run verification on a real device/emulator (Gradle wrapper jar generated by
  Android Studio on first open; not committed). No CI for the Android module yet.
- Edit-memory UI is minimal (delete/trust/audit wired; inline edit deferred).
- True Inter font files not bundled (platform sans used; drop-in path documented).
- ViewModel-level unit tests beyond the two pure-logic suites.

---

## (Archived) Phase 7 — Admin Console ✅ complete

### Goal
Deliver an **inspectable UI over all signals** — memory, retrieval explainability,
trust, and live SLOs — as a production-stable web console, reusing the official SDK
and the design system, without touching engine code.

### Deliverables
- [x] **`console/` SPA** (Vite + React + TS, ADR-015) reusing `@scp/memory-sdk`;
  React Query (fetch/cache/state) + React Router (six screens); design tokens
  (Inter, 8-pt grid, light+dark, semantic state/trust colors) in `src/theme/`
- [x] **Dashboard** — liveness + readiness + memory/intelligence/retrieval/trust
  counters and API SLOs (error rate, p50/p95/p99) parsed from `/metrics`
- [x] **Memory Explorer** — filter/page/create/delete + per-memory fields, metadata,
  and the append-only audit trail; dedup/decay passes
- [x] **Retrieval Inspector** — per-signal score bars + weights + trust (explainability)
- [x] **Trust Explorer** — provenance/confidence/freshness bars + explanation
- [x] **Benchmarks** — live per-endpoint latency percentiles vs SLO targets
- [x] **Settings** — namespace / engine URL / actor / theme (persisted)
- [x] Required Empty/Loading/Error/keyboard/a11y states on every screen;
  `docs/phase-7-admin-console.md`

### Exit Criteria (all met)
- **Inspectable UI over all signals** — six screens cover CRUD+audit, retrieval
  explainability, trust, and SLOs; explainability is visual via the score-bar primitive.
- TypeScript: `tsc` strict typecheck clean; **8 console tests** (Prometheus parser +
  quantile/error-ratio math; UI-primitive rendering); production build green (~74 kB gz).
- Data contracts verified against a live engine (`/health/ready`, CRUD/list,
  retrieval, `/v1/trust/{id}`, `/metrics`).
- Strict modularity: largest console file 233 lines (none > 300).
- **Zero engine changes** — same-origin transport (Vite proxy / reverse proxy).

### Status
**Complete** (pending user confirmation). Awaiting approval to start Phase 8.

### Key decisions
- **Reuse the SDK as the only client** (DRY) — the console exercises the same
  contract as any consumer; ops-only endpoints use a thin fetch helper.
- **Same-origin, no CORS** — relative paths proxied in dev, reverse-proxied in prod;
  the engine's security posture is untouched ([16-security-model](16-security-model.md)).
- **In-console SLIs parse `/metrics` client-side** (no Grafana dependency for the
  Dashboard/Benchmarks numbers). Console versioned independently (**0.6.0**).

---

## Production hardening (post-Phase-7, 2026-06-20)

Authorized deferred-backlog hardening on the Phases 1–6 engine. **No new phase
started**; all additions are behind seams/flags with the prior default path
unchanged. Delivered:

- [x] **Trust corroboration seam** — `RelationDetector` (lexical default, `SCP_TRUST_NLI`
  opt-in cross-encoder); behaviour-preserving refactor of `trust_service`.
- [x] **Trust calibration harness** — `evals/` Brier/ECE over a fixed labelled set;
  gates the lexical→NLI swap (24-known-risks R3).
- [x] **Keyword backend seam** — in-process BM25 default; **SQLite FTS5** (unit-tested)
  and **Postgres tsvector** inverted-index scale paths (`SCP_KEYWORD_BACKEND`).
- [x] **Weighted-vs-RRF benchmark** — `evals/` nDCG/MRR; confirms weighted is the
  better default on the eval set (nDCG 1.00 vs 0.69).
- [x] **Per-stage retrieval spans** — `observability/spans.py`, no-op-safe.
- [x] **Alert routing** — Alertmanager (severity page/ticket + inhibition); Prometheus
  wired; assets tested.
- [x] **Qdrant + Postgres in CI** — dedicated `integration` job + env-gated tests.
- [x] **Publish runbook** — `Publish_Guide.md` (PyPI/npm + async Python client).

Gates: **132 Python tests pass** (+1 benchmark, +3 gated integration skipped
offline); `ruff` + `black` clean; strict modularity held (largest new/changed file
194 lines). Nothing committed yet (Phases 1–7 + this hardening all on `master`).

**Pending (not started, still gated):** run NLI under calibration to decide adoption;
turn on FTS5/tsvector in a real deploy; publish SDKs + build the async client;
alert-receiver wiring to a real pager.

---

## Leverage / distribution artifacts (2026-06-21, user-directed)

User ran the "Pareto leverage" prompt to package the finished engineering into a
high-leverage, **Principal-level / FAANG-visibility** artifact set. This is a
**documentation + distribution workstream, not a new engine phase** — engine `src/` is
**unchanged**; the engine seeder is a thin HTTP-API client over the §29 contract.

Delivered in `scp-memory-core`:
- `documents/` — 18 strategy/architecture/career docs (analysis → Pareto → 80/20 set →
  thesis, architecture, ADRs, README positioning, benchmark, case study, blog plan, demo
  pack, talk deck, roadmap, reusable assets, career + startup leverage, 30-day plan) +
  `golden-examples.json` (the 10 Golden Examples, shared dataset).
- `seed/seed_golden_examples.py` + `seed/README.md` — engine seeder via HTTP API
  (`--in-process` / `--reset`); **validated in-process** (10/10 created; trust-scored
  verify; reset clean).
- Root `Quick_Stater_Guide.md` (newbie zero-to-run) and `blog_series/` (7 publish-ready
  Medium posts with mermaid).

Delivered in `SCPMemoryEngine_AndroidReferenceApp`:
- `seed/seed_golden_device.py` — Android Room seeder consuming the **same** shared JSON
  (direct write, backdated ages so freshness/decay is visible, namespace-scoped `--reset`);
  data pipeline validated off-device (10 memories, `derived_from` preserved).

Decisions taken this turn (user choices): seed **both** DBs from one shared JSON;
**Principal**-level framing; **all** blog posts written in full. Honest caveat documented:
the engine path mutes freshness (`recorded_at=now`) and uses the hermetic stand-in embedder,
so the contradiction-**ranking** demo is reliable on-device / with real embeddings, while
trust **scores** are meaningful everywhere.

**Not committed.** Phase 8 (Android app) remains the active engineering phase; remaining
Phase-8 work (device build/run verification) is unchanged.

## ⛔ Stop Rule (operating model)

> One phase is active at a time. Phase 8 (Android Reference App) is **now active**
> with explicit user approval (2026-06-20). Never work ahead into a *further* phase
> or skip phases. At the end of any phase: update `07`, `08`, `28`, then **stop** and
> wait for explicit instruction.

## Phase 8 status

In progress in `SCPMemoryEngine—AndroidReferenceApp` (see the Active Phase section
above). Engine code in `scp-memory-core` is **unchanged** by Phase 8 — the app is a
client over the existing §9 contract (`29-api-contracts.md`). The build spec
`android-app-system-prompt.md` is the implementation source of truth.

**Next after Phase 8:** device build/run verification, then any follow-on the user
directs. Nothing committed without authorization.

## Related

[07-current-state](07-current-state.md) · [28-session-handoff](28-session-handoff.md) · [23-roadmap](23-roadmap.md) · [../docs/phase-7-admin-console.md](../docs/phase-7-admin-console.md)
